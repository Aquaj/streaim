<h1>HELLO AÏM</h1>
<h2>ENJOY YOUR AUDIOBOOKS</h2>

<ul>
  <% @directories.sort.each do |book| %>
    <li>
      <%= link_to book, audiobook_path(book) %>
    </li>
  <% end %>
  <% @audiobooks.sort.each do |book| %>
    <li>
      <%= book %>
    </li>
  <% end %>
</ul>
